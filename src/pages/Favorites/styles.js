import styled from 'styled-components/native';

import {AUX_COLOR_3} from '../../utils/constants';

export const Container = styled.View`
    background: ${AUX_COLOR_3};
    flex: 1;
    align-items: center;
`;
